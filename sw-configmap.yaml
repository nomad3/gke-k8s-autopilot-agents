apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-sw-config
  namespace: frontend
data:
  sw.js: |
    self.addEventListener('install', (event) => {
      self.skipWaiting();
    });

    self.addEventListener('activate', (event) => {
      event.waitUntil(clients.claim());
    });

    self.addEventListener('fetch', (event) => {
      // Pass through requests
    });
