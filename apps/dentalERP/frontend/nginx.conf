server {
  listen 80;
  server_name _;

  # Serve static files
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback - serve index.html for all routes
  # The host nginx will handle /api/ and /socket.io/ proxying
  location / {
    try_files $uri $uri/ /index.html;
  }
}
